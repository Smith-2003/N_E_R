<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSV Data Table</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 20px;
            background: linear-gradient(to right, #f5f7fa, #c3cfe2);
        }

        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            background-color: #ffffff;
            border-bottom: 1px solid #ddd;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            z-index: 1000;
            position: fixed;
            top: 0;
            left: 0;
            padding: 15px 30px;
        }

        .navbar a {
            color: #0e67db;
            text-decoration: none;
            padding: 12px 20px;
            border: 2px solid transparent;
            border-radius: 6px;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .navbar a:hover {
            background-color: #e9ecef;
            border-color: #0e67db;
            color: #333;
            transform: scale(1.05);
        }

        .navbar img {
            height: 40px;
            width: auto;
        }

        .nav-links {
            display: flex;
            gap: 20px;
            align-items: center;
        }

        h1 {
            color: #0e67db;
            margin: 80px 0 40px; /* Adjusted for navbar height */
            font-size: 36px;
            text-align: center;
        }

        table {
            width: 90%;
            max-width: 1200px;
            border-collapse: collapse;
            background-color: #ffffff;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
            margin-top: 20px;
        }

        thead {
            background-color: #0e67db;
            color: #ffffff;
        }

        th, td {
            padding: 15px 20px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            font-weight: bold;
        }

        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tbody tr:hover {
            background-color: #f1f1f1;
        }

        tbody td a {
            color: #0e67db;
            text-decoration: none;
            font-weight: bold;
        }

        tbody td a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="navbar">
        <a href="/"><img src="https://symbl.ai/wp-content/uploads/2022/08/What-Is-Named-Entity-Recognition-NER-and-What-Can-You-Do-with-It.png" alt="Home"></a>
        <div class="nav-links">
            <a href="/login">Log In</a>
            <a href="/signup">Sign Up</a>
        </div>
    </div>

    <h1>The Data Table You’ve Been Waiting For</h1>
    <table>
        <thead>
            <tr>
                {{#each headers}}
                <th>{{this}}</th>
                {{/each}}
                <th>Image</th>
            </tr>
        </thead>
        <tbody>
            {{#each rows}}
            <tr>
                {{#each ../headers}}
                <td>{{lookup ../this this}}</td>
                {{/each}}
                <td>
                    <a href="{{this.imageFilename}}" target="_blank">View Image</a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>

</html>
C:\Users\khand\Desktop\GitHub\N_E_R\uploads\internship 2023-24_page-0001.jpg